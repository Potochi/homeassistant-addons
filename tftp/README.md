# TFTP Server

TFTP server for Home Assistant that serves files from a folder inside
`/share` (default `/share/tftp`), so you can manage the served files over
NFS or Samba. Supports optional uploads and per-transfer logging.

See [DOCS.md](DOCS.md) for configuration.
